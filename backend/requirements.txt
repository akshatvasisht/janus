# Core Audio Processing
faster-whisper>=1.0.0
pyaudio>=0.2.14
aubio>=0.4.9
numpy>=1.24.0

# Voice Activity Detection
# CPU-only PyTorch for wider hardware compatibility
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
silero-vad>=4.0.0

# Edge AI Stack (Local Inference)
# Use the official Qwen3-TTS wrapper. It still downloads weights from the
# Hugging Face Hub, but avoids relying on Transformers' AutoConfig mapping
# for the very new `qwen3_tts` model_type.
qwen-tts==0.0.5
# NOTE: `qwen-tts` pins its own `transformers` / `accelerate` versions.
# Avoid declaring separate constraints here to keep installs reproducible and
# prevent confusing resolver behavior.
scipy
soundfile
librosa
bitsandbytes  # Optional: Required for 4-bit quantization on CUDA devices (Linux/Windows only; MacOS MPS uses standard float16)

# Serialization
msgpack>=1.0.7

# Web Framework & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# WebSocket Communication
python-socketio>=5.10.0

# Environment Management
python-dotenv>=1.0.0

# Terminal UI
rich>=13.7.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0
pytest-timeout>=2.2.0

